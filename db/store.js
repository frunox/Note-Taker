// create a class Note

class Note {
    addNote() {
        const { title, text } = newNote;
        if (!title || !text) {
            throw new Error("Need a title and/or note text");
        } else {

        }
    }
    readNote() {
        return
    }
}